# 규칙
1. 각자 AI로 주제 하나 정해서 적당히 조사해오기
2. 조사해온 주제로 면접관 하기 
   - 적당한 선에서 꼬리질문 해주기

(조사 방법)

- 딥다이브 금지 🚫
  - AI 질문 3뎁스 내로 
  - 정답이 정해진 이론기반
- MD 파일로 조사내용 적당히 가져오기 AI한테 물어서 긁든 맘대로

# 주제 : 자바
아래 5개는 자바 백엔드 면접에서 우선순위가 높은 질문이야.
각 질문은 기본 답변 + 꼬리질문 + 꼬리질문 답변 방향으로 정리할게.

---

# 1. JVM은 무엇이고, 자바 코드는 어떻게 실행되나요?

기본 답변

JVM(Java Virtual Machine)은 자바 바이트코드를 실행하는 가상 머신입니다.

자바 코드는 바로 운영체제에서 실행되는 기계어로 컴파일되는 것이 아니라, 먼저 .class 파일인 바이트코드로 컴파일됩니다.
```
Java source code(.java)
  ↓ javac
Java bytecode(.class)
  ↓ JVM
Machine code
  ↓ CPU 실행
```
JVM은 바이트코드를 읽고, 클래스 로딩, 메모리 관리, GC(Garbage Collection), JIT(Just-In-Time) 컴파일 등을 수행합니다.

자바가 플랫폼 독립적이라고 하는 이유는 운영체제별로 JVM만 다르게 제공되면, 동일한 바이트코드를 여러 환경에서 실행할 수 있기 때문입니다.

### 꼬리질문

#### Q1. JRE와 JDK의 차이는 무엇인가요?

JRE(Java Runtime Environment)는 자바 프로그램 실행 환경입니다. JVM과 표준 라이브러리를 포함합니다.
JDK(Java Development Kit)는 개발 도구까지 포함한 패키지입니다. javac, javadoc, 디버깅 도구 등이 들어 있습니다.

- JDK = JRE + 개발 도구
- JRE = JVM + 표준 라이브러리

#### Q2. 바이트코드는 무엇인가요?

- 바이트코드는 JVM이 실행할 수 있는 중간 코드입니다.
- 운영체제나 CPU(Central Processing Unit)에 직접 종속된 기계어가 아니라, JVM 명령어 집합으로 구성된 코드입니다.

#### Q3. JIT 컴파일은 무엇인가요?

- JIT(Just-In-Time) 컴파일은 자주 실행되는 바이트코드를 실행 중에 기계어로 컴파일해서 성능을 높이는 기술입니다.
- 처음부터 모든 코드를 기계어로 바꾸는 것이 아니라, 실행 패턴을 보고 자주 사용되는 코드 중심으로 최적화합니다.

⸻

# 2. JVM 메모리 구조를 설명해주세요.

기본 답변

JVM 메모리는 크게 다음 영역으로 나눌 수 있습니다.

| 영역 | 설명 | 공유 여부 |
|---|---|---|
| Method Area | 클래스 정보, static 변수, 상수 정보 저장 | 스레드 공유 |
| Heap | 객체와 배열 저장 | 스레드 공유 |
| Stack | 메서드 호출 프레임, 지역 변수, 매개변수 저장 | 스레드별 생성 |
| PC Register | 현재 실행 중인 명령어 주소 저장 | 스레드별 생성 |
| Native Method Stack | 네이티브 메서드 실행 정보 저장 | 스레드별 생성 |

객체는 보통 Heap에 생성되고, 메서드 호출과 지역 변수는 Stack에 저장됩니다.

예를 들어:

User user = new User();

이 코드에서 new User()로 생성된 객체는 Heap에 저장되고, 지역 변수 user는 Stack의 현재 메서드 프레임에 저장됩니다. user 변수 안에는 Heap 객체를 가리키는 참조값이 들어갑니다.

### 꼬리질문

#### Q1. Stack과 Heap의 차이는 무엇인가요?

- Stack은 메서드 호출 단위로 생성되는 지역 데이터 저장 공간입니다.
- Heap은 객체가 저장되는 공간이고, 여러 스레드가 공유합니다.

- Stack: 지역 변수, 매개변수, 메서드 호출 정보
- Heap: new로 생성된 객체, 배열

#### Q2. static 변수는 어디에 저장되나요?

- static 변수는 클래스 단위로 관리되며, JVM의 Method Area에 해당하는 영역에 저장됩니다.
- HotSpot JVM 기준으로는 Java 8 이후 클래스 메타데이터는 Metaspace에 저장되고, static 필드 값은 Heap에 있는 Class 객체와 연관되어 관리됩니다.

- 면접에서는 “static은 인스턴스가 아니라 클래스 단위로 관리된다”는 점을 설명하는 것이 중요합니다.

#### Q3. StackOverflowError와 OutOfMemoryError의 차이는 무엇인가요?

- StackOverflowError는 메서드 호출이 너무 깊어져 스레드의 Stack 공간이 초과될 때 발생합니다. 대표적으로 종료 조건 없는 재귀 호출이 있습니다.

- OutOfMemoryError는 Heap이나 Metaspace 등 JVM 메모리 영역이 부족할 때 발생합니다. 예를 들어 객체를 계속 생성해서 GC가 회수하지 못하면 Heap OutOfMemoryError가 발생할 수 있습니다.

⸻

# 3. GC는 무엇이고 왜 필요한가요?

기본 답변

GC(Garbage Collection)는 JVM이 더 이상 사용되지 않는 객체를 자동으로 찾아 메모리에서 제거하는 기능입니다.

자바에서는 개발자가 직접 free()처럼 메모리를 해제하지 않습니다. 대신 객체가 더 이상 참조되지 않으면 GC 대상이 됩니다.
```
User user = new User();
user = null;
```

위 코드에서 new User() 객체를 참조하던 변수가 사라지면, 해당 객체는 더 이상 접근할 수 없으므로 GC 대상이 될 수 있습니다.

GC가 필요한 이유는 Heap 메모리를 자동으로 관리해서 메모리 누수를 줄이고, 개발자가 비즈니스 로직에 집중할 수 있게 하기 위해서입니다.

### 꼬리질문

#### Q1. GC 대상 객체는 어떻게 판단하나요?

- JVM은 보통 Reachability Analysis를 사용합니다.
- GC Root에서 시작해서 참조를 따라갈 수 있는 객체는 살아 있는 객체로 판단하고, 도달할 수 없는 객체는 GC 대상이 됩니다.

GC Root에는 보통 다음이 포함됩니다.

- Stack의 지역 변수와 매개변수가 참조하는 객체
- static 변수가 참조하는 객체
- JNI(Java Native Interface) 참조 객체
- 실행 중인 스레드가 참조하는 객체

#### Q2. Minor GC와 Major GC의 차이는 무엇인가요?

- Minor GC는 Young Generation 영역에서 발생하는 GC입니다.
- Major GC 또는 Full GC는 Old Generation을 포함한 더 넓은 영역에서 발생하는 GC를 의미합니다.

일반적으로 Minor GC는 자주 발생하지만 비교적 빠르고, Full GC는 비용이 크고 애플리케이션 정지 시간이 길어질 수 있습니다.

#### Q3. Stop-The-World는 무엇인가요?

- Stop-The-World는 GC 수행 중 애플리케이션 스레드가 일시 정지되는 현상입니다.
- GC가 객체 참조 관계를 안전하게 분석하거나 메모리를 정리하기 위해 애플리케이션 실행을 멈추는 구간이 생길 수 있습니다.

⸻

# 4. 객체지향 프로그래밍의 특징을 설명해주세요.

기본 답변

- 객체지향 프로그래밍은 프로그램을 객체들의 협력으로 구성하는 방식입니다.
- 자바는 대표적인 객체지향 언어이고, 핵심 특징은 캡슐화, 상속, 다형성, 추상화입니다.


| 특징 | 설명 |
|---|---|
| 캡슐화 | 데이터와 메서드를 하나로 묶고, 외부 접근을 제어 |
| 상속 | 기존 클래스의 속성과 기능을 재사용하거나 확장 |
| 다형성 | 같은 타입으로 여러 구현 객체를 다룰 수 있음 |
| 추상화 | 공통된 속성과 동작을 인터페이스나 추상 클래스로 표현 |

예를 들어 Payment 인터페이스를 만들고 CardPayment, KakaoPayPayment가 구현하면, 서비스 코드는 구체 클래스가 아니라 Payment 타입에 의존할 수 있습니다.
```
interface Payment {
    void pay(int amount);
}
class CardPayment implements Payment {
    public void pay(int amount) {
        // 카드 결제
    }
}
class KakaoPayPayment implements Payment {
    public void pay(int amount) {
        // 카카오페이 결제
    }
}
```
이렇게 하면 새로운 결제 방식이 추가되어도 기존 서비스 코드 변경을 줄일 수 있습니다.

### 꼬리질문

#### Q1. 오버로딩과 오버라이딩의 차이는 무엇인가요?

오버로딩은 같은 이름의 메서드를 매개변수 타입이나 개수를 다르게 해서 여러 개 정의하는 것입니다.

오버라이딩은 부모 클래스나 인터페이스의 메서드를 자식 클래스가 재정의하는 것입니다.

| 구분 | 오버로딩 | 오버라이딩 |
|---|---|---|
| 기준 | 같은 클래스 내 메서드 시그니처 차이 | 상속 관계에서 재정의 |
| 결정 시점 | 컴파일 시점 | 런타임 시점 |
| 목적 | 같은 기능 이름으로 다양한 입력 처리 | 부모 동작을 자식에 맞게 변경 |

#### Q2. 인터페이스와 추상 클래스의 차이는 무엇인가요?

- 인터페이스는 구현해야 할 기능의 계약을 정의하는 데 적합합니다.
- 추상 클래스는 공통 상태와 공통 구현을 함께 제공할 때 적합합니다.

자바에서는 클래스 다중 상속은 불가능하지만, 인터페이스는 여러 개 구현할 수 있습니다.
```
class Service extends AbstractService implements Runnable, Closeable {
}
```
#### Q3. 다형성이 왜 중요한가요?

- 다형성을 사용하면 구체 클래스가 아니라 추상 타입에 의존할 수 있습니다.
- 그 결과 구현체를 교체하거나 확장할 때 기존 코드 변경을 줄일 수 있습니다.

이 원칙은 SOLID의 OCP(Open-Closed Principle), DIP(Dependency Inversion Principle)와도 연결됩니다.

---

# 5. equals와 hashCode는 왜 같이 재정의해야 하나요?

기본 답변

equals()는 두 객체가 논리적으로 같은지 비교하는 메서드이고, hashCode()는 객체를 정수 해시값으로 변환하는 메서드입니다.

자바의 규약상 equals()가 true인 두 객체는 반드시 같은 hashCode() 값을 가져야 합니다.

이 규약이 중요한 이유는 HashMap, HashSet 같은 해시 기반 컬렉션이 객체를 저장하고 찾을 때 hashCode()와 equals()를 함께 사용하기 때문입니다.

예를 들어 HashSet은 먼저 hashCode()로 저장 위치를 찾고, 같은 해시 버킷 안에서 equals()로 실제 같은 객체인지 확인합니다.
```
Set<User> users = new HashSet<>();
users.add(new User(1L, "Kim"));
users.add(new User(1L, "Kim"));
```
equals()만 재정의하고 hashCode()를 재정의하지 않으면, 논리적으로 같은 객체인데도 다른 해시값을 가져서 중복 저장될 수 있습니다.

### 꼬리질문

#### Q1. ==와 equals()의 차이는 무엇인가요?

- ==는 기본적으로 두 참조 변수가 같은 객체를 가리키는지 비교합니다.
- equals()는 객체의 논리적 동등성을 비교하기 위해 재정의할 수 있는 메서드입니다.
```
String a = new String("java");
String b = new String("java");
System.out.println(a == b);      // false
System.out.println(a.equals(b)); // true
```
#### Q2. HashMap은 key를 어떻게 찾나요?

HashMap은 key의 hashCode()를 사용해서 버킷 위치를 찾고, 같은 버킷 안에서 equals()로 실제 key가 같은지 비교합니다.

1. key.hashCode() 계산
2. 배열 index 계산
3. 해당 bucket 탐색
4. equals()로 key 비교
5. value 반환

#### Q3. hashCode가 같으면 equals도 true인가요?

아니다.
hashCode()가 같은 객체라도 equals()는 false일 수 있습니다. 이를 해시 충돌이라고 합니다.

반대로 equals()가 true라면 hashCode()는 반드시 같아야 합니다.

equals true  → hashCode 반드시 같아야 함
hashCode 같음 → equals true일 필요 없음