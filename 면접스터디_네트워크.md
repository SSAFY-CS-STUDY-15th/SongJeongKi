# 규칙
1. 각자 AI로 주제 하나 정해서 적당히 조사해오기
2. 조사해온 주제로 면접관 하기 
   - 적당한 선에서 꼬리질문 해주기

(조사 방법)

- 딥다이브 금지 🚫
  - AI 질문 3뎁스 내로 
  - 정답이 정해진 이론기반
- MD 파일로 조사내용 적당히 가져오기 AI한테 물어서 긁든 맘대로

# 주제 : 네트워크

---
# **1. 네트워크 기본 계층**

## **Q1. OSI 7계층에 대해 설명해주세요.**

**답변**

OSI(Open Systems Interconnection) 7계층은 네트워크 통신 과정을 역할별로 나눈 모델이다.

| **계층** | **이름**             | **핵심 기준**        | **예시**           |
| ------ | ------------------ | ---------------- | ---------------- |
| 7계층    | Application Layer  | 사용자 애플리케이션 프로토콜  | HTTP, MQTT, DNS  |
| 6계층    | Presentation Layer | 데이터 형식, 인코딩, 암호화 | TLS, JSON, UTF-8 |
| 5계층    | Session Layer      | 세션 관리            | 로그인 세션, 연결 유지    |
| 4계층    | Transport Layer    | 프로세스 간 데이터 전송    | TCP, UDP         |
| 3계층    | Network Layer      | IP 기반 라우팅        | IP, ICMP         |
| 2계층    | Data Link Layer    | 같은 네트워크 내 프레임 전달 | Ethernet, Wi-Fi  |
| 1계층    | Physical Layer     | 전기/광 신호 전송       | 케이블, 무선 신호       |

| **계층** | **이름**             | **핵심 기준**        | **데이터 단위**         | **대표 장비/프로토콜**          |
| ------ | ------------------ | ---------------- | ------------------ | ----------------------- |
| L7     | Application Layer  | 애플리케이션 프로토콜      | Data               | HTTP, DNS, MQTT, SMTP   |
| L6     | Presentation Layer | 데이터 형식, 인코딩, 암호화 | Data               | TLS, UTF-8, JSON, JPEG  |
| L5     | Session Layer      | 세션 관리            | Data               | Session, RPC            |
| L4     | Transport Layer    | Port 번호          | Segment / Datagram | TCP, UDP                |
| L3     | Network Layer      | IP 주소            | Packet             | IP, ICMP, Router        |
| L2     | Data Link Layer    | MAC 주소           | Frame              | Ethernet, Wi-Fi, Switch |
| L1     | Physical Layer     | 전기/광/무선 신호       | Bit                | Cable, Hub, Repeater    |

실무에서는 보통 **TCP/IP 4계층 모델**을 더 많이 사용한다.

| **TCP/IP 계층**  | **대응 OSI 계층** |
| -------------- | ------------- |
| Application    | 5~7계층         |
| Transport      | 4계층           |
| Internet       | 3계층           |
| Network Access | 1~2계층         |
|                |               |

**꼬리 질문**

- HTTP는 몇 계층인가? : 7
- TCP는 몇 계층인가? : 4
- IP는 몇 계층인가? : 3
- 로드밸런서는 몇 계층에서 동작하는가?
	- 로드밸런서는 어떤 방식으로 트래픽을 분산하느냐에 따라 동작 계층이 다르지만, 주로 **L4(Transport Layer)** 또는 **L7(Application Layer)** 에서 동작한다고 답할 수 있다.
	- L4 로드밸런서는 Transport Layer에서 IP, Port, TCP/UDP 정보를 기준으로 연결을 분산하고, L7 로드밸런서는 Application Layer에서 HTTP Method, URI, Header, Cookie 같은 요청 내용을 해석해서 요청 단위로 라우팅한다.
- MQTT는 무엇인가?
	- Message Queuing Telemetry Transport의 줄임말
	- MQTT는 MQTT 브로커를 사이에 두고 **publish/subscribe**, 즉 **발행/구독 구조**를 사용한다.
	- MQTT는 MQTT라는 자체 패킷 형식을 가진 애플리케이션 계층 프로토콜이다.
	- 그렇다면 RabbitMQ, Kafka는 MQTT의 브로커인가? 비슷해 보이는데 무엇이 다른가?
		- 겉으로 보면 모두 “메시지를 전달하는 시스템”처럼 보인다. 하지만 정확히는 서로 **층위와 목적**이 다르다.
		- MQTT는 **프로토콜**이고, MQTT Broker는 이 프로토콜을 구현해서 `CONNECT`, `SUBSCRIBE`, `PUBLISH`, `PUBACK` 같은 MQTT 패킷을 처리하는 서버다.
		- RabbitMQ는 기본적으로 **AMQP(Advanced Message Queuing Protocol)**라는 별도의 프로토콜을 사용하는 **메시지 브로커**다.
		- RabbitMQ는 플러그인을 통해 MQTT 클라이언트도 받을 수 있지만, 기본 정체성은 MQTT Broker라기보다 **AMQP 기반 메시지 브로커**에 가깝다.
		- 반면 Kafka는 메시지 큐처럼 사용할 수는 있지만, 기본적으로는 **이벤트 스트리밍 플랫폼**이다.
		- Kafka는 AMQP를 사용하는 것도 아니고 MQTT를 사용하는 것도 아니라, **Kafka 자체 바이너리 프로토콜**을 사용한다. 따라서 Kafka 또한 MQTT Broker가 아니다.
		- 결론적으로 RabbitMQ, Kafka, MQTT Broker는 모두 애플리케이션 사이의 데이터 전달을 중간에서 처리한다는 구조적 목적은 비슷하다.
		- 하지만 RabbitMQ와 Kafka가 기본적으로 MQTT 프로토콜을 사용하는 것은 아니다.
		- RabbitMQ는 메시지 큐/라우팅 중심, Kafka는 이벤트 로그/스트리밍 중심, MQTT는 topic 기반 발행/구독과 장치 메시징 중심으로 이해하면 된다.

---
## **Q2. Socket이 무엇인가요?**

**답변**

Socket은 네트워크 통신을 위해 운영체제가 제공하는 인터페이스다.

정확히는 애플리케이션이 네트워크를 직접 다루는 것이 아니라, 운영체제의 Socket API(Application Programming Interface)를 통해 TCP 또는 UDP 통신을 수행한다.

TCP 연결은 보통 다음 4개 값으로 식별된다.

```text
출발지 IP
출발지 Port
목적지 IP
목적지 Port
```

예를 들어:

```text
클라이언트: 10.0.0.5:53021
서버:      10.0.0.10:8080
```

이 네 가지 값의 조합으로 하나의 TCP 연결이 구분된다.

**꼬리 질문**

- 서버 소켓과 클라이언트 소켓의 차이는?
	- 서보 소켓은 특정 IP와 포트에 바인딩되어 클라이언트 연결 요청을 기다리는 소켓이고
	- 클라이언트 소켓은 서버 소켓에 연결을 요청한 뒤 실제 데이터를 주고받는 소켓이다.
- 하나의 서버 포트에 여러 클라이언트가 접속할 수 있는 이유는?
	- **서버의 listening socket은 연결 요청만 받고, 실제 연결은 `클라이언트 IP(Internet Protocol):임시포트 + 서버 IP:서버포트 + TCP(Transmission Control Protocol)` 조합으로 각각 구분되는 별도 socket으로 관리되기 때문**이다.
- KeepAlive는 왜 필요한가?
	- 장시간 데이터가 오가지 않는 TCP 연결에서 실제 연결이 살아 있는지 확인하기 위해 필요합니다. 일정 시간 동안 데이터가 없으면 작은 확인 패킷을 보내 상대가 응답하는지 확인하고, 응답이 없으면 죽은 연결로 판단해 정리할 수 있습니다.

---

# **2. TCP와 UDP**

## **Q3. TCP와 UDP의 차이를 설명해주세요.**

**답변**

TCP(Transmission Control Protocol)는 **연결 지향적이고 신뢰성을 보장하는 전송 프로토콜**이다.
UDP(User Datagram Protocol)는 **연결을 설정하지 않고 데이터를 빠르게 보내는 전송 프로토콜**이다.

|**구분**|**TCP**|**UDP**|
|---|---|---|
|연결 방식|연결 지향|비연결|
|신뢰성|높음|낮음|
|순서 보장|보장|보장 안 함|
|재전송|있음|없음|
|흐름 제어|있음|없음|
|혼잡 제어|있음|없음|
|속도|상대적으로 느림|상대적으로 빠름|
|사용 예시|HTTP, HTTPS, SSH|DNS, VoIP, 게임, 영상 스트리밍|

TCP는 데이터가 정확히 도착해야 하는 경우에 적합하다.

```text
파일 다운로드
로그인
결제
REST API
```

UDP는 일부 데이터 손실보다 지연 시간이 중요한 경우에 적합하다.

```text
실시간 영상
음성 통화
게임 위치 동기화
일부 로봇 센서 데이터
```

---

## **Q4. TCP 3-Way Handshake를 설명해주세요.**

**답변**

TCP 연결을 시작할 때 클라이언트와 서버가 서로 통신 가능 상태인지 확인하는 절차다.

```text
1. 클라이언트 → 서버: SYN
2. 서버 → 클라이언트: SYN + ACK
3. 클라이언트 → 서버: ACK
```

각 단계의 의미는 다음과 같다.

```text
SYN
- Synchronize
- 연결을 시작하고 초기 sequence number를 전달한다.

ACK
- Acknowledgement
- 상대방의 패킷을 정상적으로 받았다는 확인 응답이다.
```

흐름은 다음과 같다.

```text
Client                        Server

SYN ------------------------>

     <-------------------- SYN + ACK

ACK ------------------------>
```

이후 TCP 연결이 성립되고 데이터를 주고받을 수 있다.

**꼬리 질문**
- **왜 2-Way Handshake가 아니라 3-Way Handshake인가?**  
    TCP(Transmission Control Protocol)는 양쪽이 서로의 송신 가능성과 수신 가능성을 모두 확인하고, 초기 Sequence Number를 교환해야 하므로 `SYN → SYN/ACK → ACK`의 3단계가 필요하다.
- **SYN Flood 공격은 무엇인가?**  
    SYN Flood는 공격자가 대량의 `SYN` 패킷을 보내고 마지막 `ACK(Acknowledgement)`를 보내지 않아 서버의 반쯤 열린 연결 큐를 고갈시키는 DoS(Denial of Service) 공격이다.
- **TCP 연결은 언제 ESTABLISHED 상태가 되는가?**  
    TCP 연결은 3-Way Handshake에서 클라이언트가 서버의 `SYN/ACK`에 대한 최종 `ACK`를 보내고, 서버가 그 `ACK`를 수신하면 양쪽 모두 ESTABLISHED 상태가 된다.

---

## **Q5. TCP 4-Way Handshake를 설명해주세요.**

**답변**

TCP 연결을 종료할 때 사용하는 절차다.

```text
1. 클라이언트 → 서버: FIN
2. 서버 → 클라이언트: ACK
3. 서버 → 클라이언트: FIN
4. 클라이언트 → 서버: ACK
```

흐름은 다음과 같다.

```text
Client                        Server

FIN ------------------------>

     <-------------------- ACK

     <-------------------- FIN

ACK ------------------------>
```

TCP는 양방향 통신이기 때문에 한쪽이 “나는 더 이상 보낼 데이터가 없다”고 해도, 상대방은 아직 보낼 데이터가 남아 있을 수 있다.  
그래서 종료 과정이 4단계로 나뉜다.

**꼬리 질문**
- **TIME_WAIT 상태는 왜 필요한가?**  
    TIME_WAIT은 TCP(Transmission Control Protocol) 연결 종료 후 지연 패킷이 새 연결에 섞이는 것을 막고, 마지막 ACK(Acknowledgement)가 유실됐을 때 상대방의 FIN 재전송에 다시 응답하기 위해 필요하다.
- **CLOSE_WAIT이 많이 쌓이면 어떤 문제가 있는가?**  
    CLOSE_WAIT이 많이 쌓였다는 것은 상대가 연결 종료를 요청했는데 애플리케이션이 socket close를 하지 않고 있다는 뜻이라, file descriptor와 메모리가 누수되어 신규 연결 처리에 문제가 생길 수 있다.
- **FIN과 RST의 차이는?**  
    FIN은 정상적인 TCP 연결 종료 요청이고, RST(Reset)는 비정상 상황에서 연결을 즉시 강제로 끊는 패킷이다.

---
# **3. HTTP와 HTTPS**

## **Q6. HTTP는 무엇인가요?**

**답변**

HTTP(HyperText Transfer Protocol)는 클라이언트와 서버가 요청과 응답을 주고받기 위한 애플리케이션 계층 프로토콜이다.

기본 구조는 다음과 같다.

```text
클라이언트 → 서버: HTTP Request
서버 → 클라이언트: HTTP Response
```

예를 들어 클라이언트가 다음 요청을 보낸다.

```http
GET /robots/1/status HTTP/1.1
Host: api.example.com
```

서버는 응답한다.

```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "robotId": 1,
  "status": "ACTIVE"
}
```

HTTP는 기본적으로 **stateless**, 즉 서버가 각 요청을 독립적으로 처리하는 구조다.

**꼬리 질문**

- HTTP가 stateless하다는 것은 무슨 뜻인가?
	- HTTP가 stateless하다는 것은 서버가 이전 요청의 상태를 프로토콜 자체에서 기억하지 않으며, 각 요청은 독립적으로 처리되기 때문에 인증 정보나 세션 식별자 같은 상태 정보는 Cookie, Token, Session ID 등을 통해 매 요청마다 전달해야 한다는 뜻이다.

---

## **Q7. HTTPS는 HTTP와 무엇이 다른가요?**

**답변**

HTTPS(HyperText Transfer Protocol Secure)는 HTTP에 TLS(Transport Layer Security)를 적용한 프로토콜이다.
TLS 이전에는 레거시 보안 프로토콜로 SSL 이 사용되었지만 현재는 SSL의 후속 표준인 TLS를 사용한다.
HTTP는 데이터를 평문으로 전송한다.

```text
클라이언트 → 서버: password=1234
```

중간에서 패킷을 보면 내용이 노출될 수 있다.

  

HTTPS는 TLS를 통해 다음을 제공한다.

```text
1. 암호화
2. 서버 인증
3. 데이터 무결성 검증
```

| **기능** | **설명**                 |
| ------ | ---------------------- |
| 암호화    | 중간에서 내용을 볼 수 없게 함      |
| 서버 인증  | 접속한 서버가 진짜인지 인증서로 확인   |
| 무결성    | 전송 중 데이터가 변조되지 않았는지 확인 |

**꼬리 질문**

- HTTPS에서도 IP와 포트는 필요한가?
	- 당연히 필요하다. HTTPS도 결국 TCP 위에서 동작하므로 IP 주소와 포트가 필요하다.

---

## **Q8. HTTP Method를 설명해주세요.**

**답변**

HTTP Method는 요청이 서버 자원에 대해 어떤 작업을 수행하려는지 나타낸다.

|**Method**|**의미**|**예시**|
|---|---|---|
|GET|조회|로봇 상태 조회|
|POST|생성 또는 명령 요청|로봇 작업 생성|
|PUT|전체 수정|로봇 설정 전체 변경|
|PATCH|부분 수정|로봇 이름만 변경|
|DELETE|삭제|작업 취소 또는 리소스 삭제|

예시:

```http
GET /robots/1
```

```text
1번 로봇 정보를 조회한다.
```

```http
POST /robots/1/commands
```

```text
1번 로봇에게 명령을 생성한다.
```

**꼬리 질문**

- GET 요청에 Body를 넣어도 되는가?
	- HTTP(HyperText Transfer Protocol) 명세상 GET Body가 완전히 금지된 것은 아니지만, 의미가 명확하지 않고 서버·프록시·클라이언트에서 무시될 수 있어 일반적으로 사용하지 않는다.
- PUT과 PATCH의 차이는?
	- PUT은 리소스 전체를 교체하는 요청이고, PATCH는 리소스의 일부 필드만 부분 수정하는 요청이다.
- POST와 PUT 중 어떤 것이 멱등인가?
	- 일반적으로 PUT은 같은 요청을 여러 번 보내도 결과가 같으므로 멱등이고, POST는 요청마다 새 리소스 생성이나 상태 변화가 발생할 수 있어 멱등이 아니다.

---
# **4. HTTP 상태 코드**

## **Q9. 주요 HTTP 상태 코드를 설명해주세요.**

**답변**

HTTP 상태 코드는 서버가 요청 처리 결과를 숫자로 표현한 값이다.

| 범위        | 의미       | 설명                                                   |
| --------- | -------- | ---------------------------------------------------- |
| **200번대** | 성공       | 클라이언트 요청을 서버가 정상적으로 이해했고, 처리도 성공했다는 의미               |
| **300번대** | 리다이렉션    | 요청한 리소스가 다른 위치에 있거나, 추가 이동/재요청이 필요하다는 의미             |
| **400번대** | 클라이언트 오류 | 요청 자체에 문제가 있거나, 클라이언트가 권한/인증/요청 형식을 잘못 보냈다는 의미       |
| **500번대** | 서버 오류    | 요청은 들어왔지만 서버 내부 문제, 서버 간 통신 실패, 과부하 등으로 처리하지 못했다는 의미 |

|**상태 코드**|**의미**|
|---|---|
|200 OK|요청 성공|
|201 Created|리소스 생성 성공|
|204 No Content|성공했지만 응답 Body 없음|
|301 Moved Permanently|영구 리다이렉트|
|302 Found|임시 리다이렉트|
|400 Bad Request|잘못된 요청|
|401 Unauthorized|인증 필요|
|403 Forbidden|권한 없음|
|404 Not Found|리소스 없음|
|409 Conflict|상태 충돌|
|500 Internal Server Error|서버 내부 오류|
|502 Bad Gateway|게이트웨이 또는 프록시 오류|
|503 Service Unavailable|서버 일시적 사용 불가|
|504 Gateway Timeout|게이트웨이 타임아웃|

---

# **5. DNS**

## **Q10. DNS가 무엇인가요?**

**답변**

DNS(Domain Name System)는 도메인 이름을 IP 주소로 변환하는 시스템이다.

사람은 보통 도메인 이름을 사용한다.

```text
api.example.com
```

하지만 네트워크 통신에는 IP 주소가 필요하다.

```text
203.0.113.10
```

DNS는 이 변환을 담당한다.

```text
api.example.com → 203.0.113.10
```

**DNS 조회 흐름**

```text
1. 브라우저 또는 OS 캐시 확인
2. 로컬 DNS 서버에 질의
3. Root DNS 서버 질의
4. TLD DNS 서버 질의
5. Authoritative DNS 서버 질의
6. IP 주소 반환
```

TLD는 Top-Level Domain의 약자로 `.com`, `.net`, `.kr` 같은 최상위 도메인을 말한다.

**꼬리 질문**

- DNS 캐시는 왜 필요한가?
	- DNS(Domain Name System) 조회 결과를 일정 시간 저장해 같은 도메인에 대한 반복 조회 지연 시간과 DNS 서버 부하를 줄이기 위해 필요하다.
- DNS 장애가 발생하면 어떤 문제가 생기는가?
	- 사용자가 도메인을 IP(Internet Protocol) 주소로 해석하지 못해 서버가 정상 동작 중이어도 웹/API(Application Programming Interface)/메일 같은 서비스에 접속하지 못할 수 있다.


---

# **6. Connection, Timeout, Keep-Alive**

## **Q11. HTTP Keep-Alive가 무엇인가요?**

**답변**

HTTP Keep-Alive는 하나의 TCP 연결을 여러 HTTP 요청/응답에 재사용하는 기능이다.

Keep-Alive가 없으면 요청마다 TCP 연결을 새로 맺어야 한다.

```text
요청 1 → TCP 연결 생성 → 응답 → 연결 종료
요청 2 → TCP 연결 생성 → 응답 → 연결 종료
요청 3 → TCP 연결 생성 → 응답 → 연결 종료
```

Keep-Alive를 사용하면 다음과 같다.

```text
TCP 연결 생성
요청 1 → 응답
요청 2 → 응답
요청 3 → 응답
TCP 연결 종료
```

장점은 다음과 같다.

```text
TCP Handshake 비용 감소
TLS Handshake 비용 감소
지연 시간 감소
서버/클라이언트 리소스 절약
```

**꼬리 질문**
- **Keep-Alive 연결을 너무 오래 유지하면 어떤 문제가 생기는가?**  
    사용하지 않는 TCP(Transmission Control Protocol) 연결이 오래 남아 서버의 file descriptor, 메모리, connection table 같은 자원을 점유하고, 중간 프록시나 로드밸런서의 idle timeout과 충돌해 끊어진 연결을 재사용하려는 문제가 생길 수 있다.
- **Connection Pool과 Keep-Alive의 관계는?**  
    Keep-Alive는 하나의 TCP 연결을 여러 요청에 재사용하게 해주는 HTTP(HyperText Transfer Protocol) 기능이고, Connection Pool은 그런 재사용 가능한 연결들을 여러 개 보관·관리하는 클라이언트/서버 측 자원 관리 구조다.
- **HTTP/2에서는 연결 재사용이 어떻게 달라지는가?**  
    HTTP/2는 하나의 TCP 연결 안에서 여러 요청/응답을 stream으로 multiplexing하므로, HTTP/1.1처럼 요청마다 여러 연결을 많이 열 필요가 줄어든다.
---

## **Q12. Timeout에는 어떤 종류가 있나요?**

**답변**

백엔드에서 자주 다루는 Timeout은 다음과 같다.

|**종류**|**의미**|
|---|---|
|Connection Timeout|연결을 맺는 데 허용하는 최대 시간|
|Read Timeout|연결 후 응답 데이터를 기다리는 최대 시간|
|Write Timeout|요청 데이터를 쓰는 데 허용하는 최대 시간|
|Idle Timeout|연결이 유휴 상태로 유지될 수 있는 최대 시간|
|Gateway Timeout|프록시 또는 게이트웨이가 백엔드 응답을 기다리는 최대 시간|

예를 들어 로봇 제어 서버가 로봇 에이전트에 명령을 보냈는데 응답이 없다면, 무한정 기다리면 안 된다.  
적절한 timeout을 설정하고 실패 처리해야 한다.

  

**로봇 SW 관점**

```text
로봇 상태 조회 timeout
명령 전송 timeout
명령 ACK timeout
작업 완료 timeout
SSE/WebSocket idle timeout
```

이런 값을 명확히 구분해야 한다.


---

## **Q13. CORS란 무엇인가요?**

**답변**

CORS(Cross-Origin Resource Sharing)는 브라우저가 다른 origin의 서버로 요청할 때 적용하는 보안 정책이다.

Origin은 다음 세 가지 조합이다.

```text
Protocol
Host
Port
```

예를 들어:

```text
http://localhost:3000
http://localhost:8080
```

두 주소는 포트가 다르므로 다른 origin이다.

  

브라우저는 다른 origin으로 요청할 때 서버가 허용했는지 확인한다.  
서버가 다음과 같은 헤더를 내려주면 허용된다.

```http
Access-Control-Allow-Origin: http://localhost:3000
```

CORS는 서버 간 통신 문제가 아니라 브라우저 보안 정책이다.  
서버에서 서버로 직접 요청할 때는 CORS가 적용되지 않는다.

**꼬리 질문**

- **CORS는 브라우저가 막는가, 서버가 막는가?**  
    CORS(Cross-Origin Resource Sharing)는 서버가 허용 정책을 응답 헤더로 알려주고, 브라우저가 그 정책을 검사해서 차단 여부를 결정한다.
- **Preflight 요청은 무엇인가?**  
    Preflight 요청은 실제 CORS 요청 전에 브라우저가 `OPTIONS` 요청을 보내 서버가 해당 Origin, Method, Header를 허용하는지 미리 확인하는 절차다.
- **쿠키를 포함한 CORS 요청에는 어떤 설정이 필요한가?**  
    클라이언트는 `credentials: include`를 설정하고, 서버는 `Access-Control-Allow-Credentials: true`와 함께 `Access-Control-Allow-Origin`에 `*`가 아닌 구체적인 Origin을 지정해야 한다.

---
